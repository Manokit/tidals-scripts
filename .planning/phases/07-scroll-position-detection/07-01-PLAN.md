---
phase: 07-scroll-position-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [utilities/src/main/java/utilities/BankScrollUtils.java, utilities/jar/TidalsUtilities.jar]
autonomous: true
---

<objective>
Add isAtTop() and isAtBottom() methods to BankScrollUtils for sprite-based scroll position detection.

Purpose: Enable verified withdrawal flow (Phase 9) to know scroll position without relying on scroll button visibility alone.
Output: Two new boolean methods in BankScrollUtils that check scrollbar sprite Y coordinates.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-scroll-position-detection/07-CONTEXT.md

@utilities/src/main/java/utilities/BankScrollUtils.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add isAtTop() and isAtBottom() methods to BankScrollUtils</name>
  <files>utilities/src/main/java/utilities/BankScrollUtils.java</files>
  <action>
Add two new public static methods:

1. `isAtTop(Script script)` - Returns true when scroll bar is at top position
   - Call init(script) first to ensure sprites loaded
   - Check if bank is visible, return false if not
   - Use ImageAnalyzer.findLocations(scrollbarTopImage) to find sprite 789
   - If matches found, check if first match Y coordinate equals 334
   - Return true if at Y=334, false otherwise

2. `isAtBottom(Script script)` - Returns true when scroll bar is at bottom position
   - Call init(script) first to ensure sprites loaded
   - Check if bank is visible, return false if not
   - Use ImageAnalyzer.findLocations(scrollbarBottomImage) to find sprite 791
   - If matches found, check if first match Y coordinate equals 507
   - Return true if at Y=507, false otherwise

Add constants for the Y coordinates at top of file:
```java
private static final int SCROLLBAR_TOP_Y = 334;
private static final int SCROLLBAR_BOTTOM_Y = 507;
```

Note: Use getAsPoint().y to extract Y coordinate from ImageSearchResult. Pattern matches existing code in getScrollbarPosition() method.
  </action>
  <verify>
Java compiles without errors:
```bash
cd tidals-scripts && JAVA_HOME=$(/usr/libexec/java_home -v 17) gradle :utilities:compileJava --console=plain 2>&1 | tail -5
```
  </verify>
  <done>isAtTop() and isAtBottom() methods added, code compiles</done>
</task>

<task type="auto">
  <name>Task 2: Build and verify TidalsUtilities.jar</name>
  <files>utilities/jar/TidalsUtilities.jar</files>
  <action>
Build the utilities module to create updated TidalsUtilities.jar:
```bash
cd tidals-scripts && JAVA_HOME=$(/usr/libexec/java_home -v 17) gradle :utilities:build
```

Verify the jar was updated by checking modification time.
  </action>
  <verify>
```bash
ls -la utilities/jar/TidalsUtilities.jar
```
Modification time should be recent.
  </verify>
  <done>TidalsUtilities.jar rebuilt with isAtTop() and isAtBottom() methods</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `gradle :utilities:compileJava` succeeds without errors
- [ ] `gradle :utilities:build` succeeds
- [ ] TidalsUtilities.jar modification time is current
- [ ] isAtTop() method exists with Y=334 check
- [ ] isAtBottom() method exists with Y=507 check
</verification>

<success_criteria>

- All tasks completed
- No compilation errors
- Methods follow existing sprite detection pattern
- Constants defined for Y coordinates (maintainability)
</success_criteria>

<output>
After completion, create `.planning/phases/07-scroll-position-detection/07-01-SUMMARY.md`
</output>
