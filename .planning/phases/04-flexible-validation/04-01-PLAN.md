---
phase: 04-flexible-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - TidalsSecondaryCollector/src/main/java/strategies/MortMyreFungusCollector.java
autonomous: true

must_haves:
  truths:
    - "Script starts with bloom item in inventory only"
    - "Script starts with ardy cloak in inventory only"
    - "Script starts with quest cape in inventory only"
    - "Script auto-equips dramen staff from inventory"
  artifacts:
    - path: "TidalsSecondaryCollector/src/main/java/strategies/MortMyreFungusCollector.java"
      provides: "Flexible equipment validation"
      contains: "autoEquipDramenStaff"
  key_links:
    - from: "verifyRequirements()"
      to: "inventory search"
      via: "check equipment first, then inventory"
    - from: "verifyRequirements()"
      to: "autoEquipDramenStaff()"
      via: "equip dramen from inventory if not equipped"
---

<objective>
Make equipment validation more permissive by accepting required items in inventory (not just equipped) and auto-equipping dramen staff at startup.

Purpose: Allow users to start the script with items in inventory rather than requiring all items pre-equipped. This is more flexible for users switching between modes.
Output: Modified verifyRequirements() with inventory fallback checks and auto-equip logic.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mode-detection/01-01-SUMMARY.md

@TidalsSecondaryCollector/src/main/java/strategies/MortMyreFungusCollector.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add quest cape constant and helper method for auto-equipping</name>
  <files>TidalsSecondaryCollector/src/main/java/strategies/MortMyreFungusCollector.java</files>
  <action>
1. Add QUEST_CAPE constant (item ID 9813) near other cape constants
2. Add autoEquipDramenStaff() helper method that:
   - Opens inventory tab
   - Searches for dramen staff (ID 772) in inventory
   - If found, uses RetryUtils.inventoryInteract with "Wield" action
   - Opens equipment tab and verifies dramen staff is now equipped
   - Returns true if successful, false otherwise
3. Place helper method near the verifyRequirements() method for logical grouping
  </action>
  <verify>Build succeeds: `osmb build TidalsSecondaryCollector`</verify>
  <done>QUEST_CAPE constant exists, autoEquipDramenStaff() method compiles</done>
</task>

<task type="auto">
  <name>Task 2: Update fairy ring mode validation to check inventory and auto-equip</name>
  <files>TidalsSecondaryCollector/src/main/java/strategies/MortMyreFungusCollector.java</files>
  <action>
Modify the fairy ring mode detection section in verifyRequirements() (around line 251-295):

1. For dramen staff check:
   - First check if equipped (existing logic)
   - If not equipped, check inventory for dramen staff
   - If found in inventory, call autoEquipDramenStaff()
   - If auto-equip fails, return false with error

2. For ardy cloak check (line 279-287):
   - First check if equipped (existing logic)
   - If not equipped, check inventory for ardy cloak
   - If found in either location, accept it (no auto-equip needed - ardy cloak works from inventory)
   - Update log messages to indicate where item was found

3. Add quest cape as alternative to ardy cloak for fairy ring mode:
   - After ardy cloak check, also check for quest cape (equipped or inventory)
   - Either ardy cloak OR quest cape satisfies the requirement
   - Update log messages accordingly
  </action>
  <verify>Build succeeds: `osmb build TidalsSecondaryCollector`</verify>
  <done>Fairy ring mode accepts items in inventory, auto-equips dramen staff</done>
</task>

<task type="auto">
  <name>Task 3: Update ver sinhaza mode validation to check inventory</name>
  <files>TidalsSecondaryCollector/src/main/java/strategies/MortMyreFungusCollector.java</files>
  <action>
Modify the ver sinhaza mode detection section in verifyRequirements() (around line 296-334):

1. For bloom tool check:
   - Keep existing equipment check
   - This should already work since ver sinhaza mode requires bloom equipped (can't be in inventory - no auto-equip for bloom tools in ver sinhaza since drakan's medallion doesn't occupy weapon slot)

2. For drakan's medallion check (line 322-327):
   - First check if equipped (existing logic)
   - If not equipped, check inventory for drakan's medallion
   - If found in inventory, note for future auto-equip consideration (for now just accept it and log warning that it should be equipped)
   - Keep strict for now - medallion must be equipped for ver sinhaza mode to work

No major changes needed for ver sinhaza mode - the bloom tool must be equipped to cast bloom from equipment, and medallion teleport requires it equipped.
  </action>
  <verify>Build succeeds: `osmb build TidalsSecondaryCollector`</verify>
  <done>Ver sinhaza mode validation unchanged (equipment required for teleport/bloom)</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `osmb build TidalsSecondaryCollector` succeeds without errors
- [ ] QUEST_CAPE constant defined with ID 9813
- [ ] autoEquipDramenStaff() method exists and compiles
- [ ] Fairy ring mode accepts dramen staff in inventory (auto-equips)
- [ ] Fairy ring mode accepts ardy cloak in inventory OR equipment
- [ ] Fairy ring mode accepts quest cape as alternative to ardy cloak
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No compilation errors
- VALID-01 through VALID-04 requirements addressed
</success_criteria>

<output>
After completion, create `.planning/phases/04-flexible-validation/04-01-SUMMARY.md`
</output>
