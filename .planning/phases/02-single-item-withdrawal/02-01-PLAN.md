---
phase: 02-single-item-withdrawal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - utilities/src/main/java/utilities/BankSearchUtils.java
  - utilities/jar/TidalsUtilities.jar
autonomous: true
---

<objective>
Implement searchAndWithdraw() method that searches for an item by name and withdraws the specified amount.

Purpose: Enable scripts to withdraw items without knowing bank tab layout - just provide the item ID and amount.
Output: Working searchAndWithdraw() method in BankSearchUtils, rebuilt JAR.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-search-infrastructure/01-02-SUMMARY.md

# Existing implementation to extend:
@utilities/src/main/java/utilities/BankSearchUtils.java
@utilities/src/main/java/utilities/BankingUtils.java

# API reference:
@docs/Bank.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement searchAndWithdraw() method</name>
  <files>utilities/src/main/java/utilities/BankSearchUtils.java</files>
  <action>
Add searchAndWithdraw(Script script, int itemId, int amount) method:

1. Pre-condition: Bank must be visible (return false if not)

2. Get item name from ID:
   - String itemName = script.getItemManager().getItemName(itemId);
   - If null/empty, log error and return false

3. Type search:
   - Call typeSearch(script, itemName)
   - If fails, return false

4. Wait for bank to filter:
   - pollFramesHuman with short delay (300-500ms) for results to update

5. Verify item exists in filtered bank:
   - ItemGroupResult bankItems = script.getWidgetManager().getBank().search(Set.of(itemId));
   - If bankItems null or !bankItems.contains(itemId), log "item not found after search", clearSearch(), return false

6. Withdraw:
   - boolean success = script.getWidgetManager().getBank().withdraw(itemId, amount);
   - Log result

7. Clear search:
   - clearSearch(script)

8. Return success

Add overload: searchAndWithdraw(Script script, int itemId, int amount, boolean keepSearchOpen)
- Same as above but skips clearSearch() if keepSearchOpen is true
- Default method calls with keepSearchOpen=false

Follow existing patterns:
- Static method with Script as first param
- Log with BankSearchUtils.class
- Human-like delays between steps
- Return boolean for success/failure
  </action>
  <verify>File compiles: cd tidals-scripts && JAVA_HOME=$(/usr/libexec/java_home -v 17) gradle :utilities:compileJava 2>&1 | tail -5</verify>
  <done>searchAndWithdraw() method implemented with both overloads, compiles without errors</done>
</task>

<task type="auto">
  <name>Task 2: Build and verify utilities JAR</name>
  <files>utilities/jar/TidalsUtilities.jar</files>
  <action>
1. Build utilities module:
   cd tidals-scripts && JAVA_HOME=$(/usr/libexec/java_home -v 17) gradle :utilities:build

2. Verify JAR contains updated class:
   jar tf utilities/jar/TidalsUtilities.jar | grep BankSearchUtils

3. Verify method is in bytecode (quick check):
   javap -classpath utilities/jar/TidalsUtilities.jar utilities.BankSearchUtils | grep searchAndWithdraw
  </action>
  <verify>JAR exists at utilities/jar/TidalsUtilities.jar, contains BankSearchUtils.class, javap shows searchAndWithdraw method</verify>
  <done>TidalsUtilities.jar built with searchAndWithdraw() method visible in bytecode</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] searchAndWithdraw(script, itemId, amount) implemented
- [ ] searchAndWithdraw(script, itemId, amount, keepSearchOpen) overload implemented
- [ ] Both methods compile without errors
- [ ] TidalsUtilities.jar built successfully
- [ ] javap confirms searchAndWithdraw in class
</verification>

<success_criteria>

- searchAndWithdraw() method works: types item name, waits for filter, withdraws by ID, clears search
- Handles error cases: bank not visible, item not found after search, withdraw fails
- Follows existing utility patterns (static methods, logging, human delays)
- JAR rebuilt and ready for use by scripts
</success_criteria>

<output>
After completion, create `.planning/phases/02-single-item-withdrawal/02-01-SUMMARY.md`
</output>
