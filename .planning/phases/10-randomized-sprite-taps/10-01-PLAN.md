---
phase: 10-randomized-sprite-taps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [utilities/src/main/java/utilities/BankSearchUtils.java, utilities/src/main/java/utilities/BankScrollUtils.java, utilities/jar/TidalsUtilities.jar]
autonomous: true
---

<objective>
Convert Point-based sprite taps to Rectangle-based taps for humanized click positions.

Purpose: Make sprite interactions appear human-like by tapping random coordinates within sprite bounds instead of the exact center point.
Output: BankSearchUtils and BankScrollUtils with randomized tap coordinates, rebuilt JAR.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@utilities/src/main/java/utilities/BankSearchUtils.java
@utilities/src/main/java/utilities/BankScrollUtils.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert Point taps to Rectangle taps in sprite detection methods</name>
  <files>utilities/src/main/java/utilities/BankSearchUtils.java, utilities/src/main/java/utilities/BankScrollUtils.java</files>
  <action>
Replace `getAsPoint()` with `getBounds()` and update tap calls in 3 locations:

1. **BankSearchUtils.tapSearchButton()** (~line 103-106):
   Change:
   ```java
   ImageSearchResult result = matches.get(0);
   Point location = result.getAsPoint();
   script.log(BankSearchUtils.class, "tapping search button at: " + location.x + "," + location.y);
   boolean tapped = script.getFinger().tap(location);
   ```
   To:
   ```java
   ImageSearchResult result = matches.get(0);
   Rectangle bounds = result.getBounds();
   script.log(BankSearchUtils.class, "tapping search button within: " + bounds);
   boolean tapped = script.getFinger().tap(bounds);
   ```

2. **BankScrollUtils.scrollDown()** (~line 137-140):
   Change:
   ```java
   ImageSearchResult result = matches.get(0);
   Point location = result.getAsPoint();
   script.log(BankScrollUtils.class, "tapping scroll down at: " + location.x + "," + location.y);
   boolean tapped = script.getFinger().tap(location);
   ```
   To:
   ```java
   ImageSearchResult result = matches.get(0);
   Rectangle bounds = result.getBounds();
   script.log(BankScrollUtils.class, "tapping scroll down within: " + bounds);
   boolean tapped = script.getFinger().tap(bounds);
   ```

3. **BankScrollUtils.scrollUp()** (~line 176-180):
   Change:
   ```java
   ImageSearchResult result = matches.get(0);
   Point location = result.getAsPoint();
   script.log(BankScrollUtils.class, "tapping scroll up at: " + location.x + "," + location.y);
   boolean tapped = script.getFinger().tap(location);
   ```
   To:
   ```java
   ImageSearchResult result = matches.get(0);
   Rectangle bounds = result.getBounds();
   script.log(BankScrollUtils.class, "tapping scroll up within: " + bounds);
   boolean tapped = script.getFinger().tap(bounds);
   ```

Note: `tap(Shape)` automatically selects a random point within the shape bounds (per Finger.md documentation). No additional randomization code needed.

Remove the `import java.awt.Point;` from BankScrollUtils.java if no longer used (keep it in BankSearchUtils.java since Point is used elsewhere like in isAtTop/isAtBottom).
  </action>
  <verify>cd utilities && JAVA_HOME=$(/usr/libexec/java_home -v 17) ../gradlew compileJava</verify>
  <done>All 3 tap calls use getBounds() instead of getAsPoint(), code compiles without errors</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild TidalsUtilities.jar</name>
  <files>utilities/jar/TidalsUtilities.jar</files>
  <action>
Build the utilities module to create updated JAR:
```bash
cd utilities && JAVA_HOME=$(/usr/libexec/java_home -v 17) ../gradlew build
```

Verify JAR was updated by checking modification time.
  </action>
  <verify>ls -la utilities/jar/TidalsUtilities.jar shows recent modification time</verify>
  <done>TidalsUtilities.jar rebuilt with randomized sprite taps</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All 3 sprite tap locations use getBounds() instead of getAsPoint()
- [ ] `cd utilities && JAVA_HOME=$(/usr/libexec/java_home -v 17) ../gradlew build` succeeds
- [ ] TidalsUtilities.jar has recent modification timestamp
- [ ] No Point import in BankScrollUtils if no longer needed
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No compilation errors
- Sprite taps now use rectangle bounds for randomized click positions
</success_criteria>

<output>
After completion, create `.planning/phases/10-randomized-sprite-taps/10-01-SUMMARY.md`
</output>
