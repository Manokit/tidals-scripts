---
phase: 01-core-search-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [utilities/src/main/java/utilities/BankSearchUtils.java]
autonomous: true
---

<objective>
Discover bank button click API and implement search box opening.

Purpose: Establish foundation for bank search functionality by confirming API behavior and creating the initial utility class.
Output: BankSearchUtils.java with openSearch() method that reliably opens the bank search box.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@utilities/src/main/java/utilities/BankingUtils.java
@utilities/src/main/java/utilities/RetryUtils.java
@docs/Bank.md
@docs/Keyboard.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Discover bank button click API</name>
  <files>N/A - research only</files>
  <action>
  Research how to click bank buttons in OSMB:

  1. Search the examples/ directory for any usage of BankButtonType or bank button clicking
  2. Search for patterns like `.clickButton`, `.tapButton`, `.selectButton`, or similar
  3. Check if Bank interface has a method for button interaction
  4. Look at how other interfaces handle button clicks (e.g., Tab buttons, Dialogue options)

  If no direct method exists, explore alternatives:
  - Can we find the button bounds via the Bank interface and use getFinger().tap()?
  - Is there a getBounds() or similar method on BankButtonType?

  Document findings in the SUMMARY.md under "Discovery Findings" section.
  </action>
  <verify>Findings documented - either confirmed API method OR alternative approach identified</verify>
  <done>Clear understanding of how to click the SEARCH button in the bank UI</done>
</task>

<task type="auto">
  <name>Task 2: Create BankSearchUtils with openSearch()</name>
  <files>utilities/src/main/java/utilities/BankSearchUtils.java</files>
  <action>
  Create BankSearchUtils.java following existing *Utils patterns (BankingUtils, RetryUtils):

  1. Package: utilities
  2. Static methods with Script as first parameter
  3. Add class-level Javadoc describing purpose

  Implement openSearch(Script script) method:
  - Pre-condition: Bank must be visible (verify with script.getWidgetManager().getBank().isVisible())
  - Click the SEARCH button using discovered API method
  - Wait for search input to be ready (use polling with timeout)
  - Return boolean: true if search box opened successfully
  - Log attempts/failures using script.log(BankSearchUtils.class, message)
  - Use RetryUtils pattern if button click is unreliable (up to 5 attempts)

  Add human-like delays (pollFramesHuman with 200-400ms) after button click.

  DO NOT implement type or clear methods yet - those are in Plan 02.
  </action>
  <verify>
  - File compiles: cd tidals-scripts && JAVA_HOME=$(/usr/libexec/java_home -v 17) gradle :utilities:compileJava
  - Class follows *Utils naming pattern
  - openSearch() has proper null checks and bank visibility check
  </verify>
  <done>
  - BankSearchUtils.java exists in utilities/src/main/java/utilities/
  - openSearch() method implemented with discovered API approach
  - Builds without errors
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Discovery findings documented (API method or alternative)
- [ ] BankSearchUtils.java created with proper structure
- [ ] openSearch() method implemented
- [ ] `JAVA_HOME=$(/usr/libexec/java_home -v 17) gradle :utilities:compileJava` succeeds
</verification>

<success_criteria>

- Discovery task produced clear answer on bank button interaction
- BankSearchUtils.java follows existing utility patterns
- openSearch() compiles and handles edge cases (bank not open)
- Ready for Plan 02 to add typing and clearing
  </success_criteria>

<output>
After completion, create `.planning/phases/01-core-search-infrastructure/01-01-SUMMARY.md`
</output>
