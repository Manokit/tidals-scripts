---
phase: 01-core-search-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [utilities/src/main/java/utilities/BankSearchUtils.java]
autonomous: true
---

<objective>
Implement search typing and clearing to complete core search infrastructure.

Purpose: Enable full search flow: open search box, type item name, and clear/reset search.
Output: Complete BankSearchUtils with typeSearch() and clearSearch() methods.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-search-infrastructure/01-01-SUMMARY.md
@utilities/src/main/java/utilities/BankSearchUtils.java
@docs/Keyboard.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement typeSearch()</name>
  <files>utilities/src/main/java/utilities/BankSearchUtils.java</files>
  <action>
  Add typeSearch(Script script, String itemName) method:

  1. Pre-condition: Search box must be open (either verify state OR call openSearch() first)
  2. Use Keyboard API to type the item name:
     - script.getKeyboard().type(itemName)
  3. Wait for search results to appear using polling (search filters bank instantly, but add small delay for visual update)
  4. Add human-like delay after typing (200-400ms pollFramesHuman)
  5. Return boolean: true if typing completed

  Handle edge cases:
  - Empty/null item name: log error, return false
  - Search not open: attempt to open it first OR return false with log

  Per Keyboard.md: "type() sends characters sequentially" and "assumes text input field is already focused"
  </action>
  <verify>grep -n "typeSearch" utilities/src/main/java/utilities/BankSearchUtils.java shows method exists</verify>
  <done>typeSearch() method implemented with proper keyboard input and error handling</done>
</task>

<task type="auto">
  <name>Task 2: Implement clearSearch()</name>
  <files>utilities/src/main/java/utilities/BankSearchUtils.java</files>
  <action>
  Add clearSearch(Script script) method:

  Research first: How does the bank search get cleared?
  - Press ESC key? (common pattern - Keyboard.pressKey(TouchType.DOWN/UP, PhysicalKey.ESCAPE))
  - Click the X button in search box?
  - Click outside search box?
  - Click search button again to toggle off?

  Implement based on findings (likely ESC key):
  1. Check if search is active (may need discovery - look for visual indicator or text presence)
  2. Clear search using appropriate method
  3. Wait for bank to show all items again (no search filter)
  4. Add human-like delay
  5. Return boolean: true if cleared successfully

  If multiple methods work, prefer the most reliable one.
  </action>
  <verify>grep -n "clearSearch" utilities/src/main/java/utilities/BankSearchUtils.java shows method exists</verify>
  <done>clearSearch() method implemented that reliably clears the search box</done>
</task>

<task type="auto">
  <name>Task 3: Build and verify utilities JAR</name>
  <files>utilities/jar/TidalsUtilities.jar</files>
  <action>
  Build the complete utilities package:

  1. Run: cd tidals-scripts && JAVA_HOME=$(/usr/libexec/java_home -v 17) gradle :utilities:build
  2. Verify JAR updated: ls -la utilities/jar/TidalsUtilities.jar
  3. Verify BankSearchUtils is included: jar -tf utilities/jar/TidalsUtilities.jar | grep BankSearchUtils

  This ensures the new utility is ready for use by scripts.
  </action>
  <verify>
  - Build succeeds without errors
  - JAR contains utilities/BankSearchUtils.class
  </verify>
  <done>TidalsUtilities.jar built and contains BankSearchUtils with all three methods</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] typeSearch() method implemented and handles empty/null input
- [ ] clearSearch() method implemented
- [ ] `JAVA_HOME=$(/usr/libexec/java_home -v 17) gradle :utilities:build` succeeds
- [ ] JAR contains BankSearchUtils.class
</verification>

<success_criteria>

- All three methods (openSearch, typeSearch, clearSearch) implemented
- BankSearchUtils follows existing *Utils patterns
- Utilities JAR builds successfully
- Phase 1 complete - ready for Phase 2 (Single Item Withdrawal)
  </success_criteria>

<output>
After completion, create `.planning/phases/01-core-search-infrastructure/01-02-SUMMARY.md`
</output>
