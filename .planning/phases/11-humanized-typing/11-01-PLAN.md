---
phase: 11-humanized-typing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [utilities/src/main/java/utilities/BankSearchUtils.java, utilities/jar/TidalsUtilities.jar]
autonomous: true
---

<objective>
Add random delays between characters when typing search queries to make typing appear human-like.

Purpose: Eliminate machine-gun typing that looks robotic. Characters should be typed with variable delays mimicking human keyboard input.
Output: typeSearch() method uses humanized typing with random per-character delays.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-randomized-sprite-taps/10-01-SUMMARY.md

# Source file to modify
@utilities/src/main/java/utilities/BankSearchUtils.java

# API reference for keyboard
@docs/Keyboard.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create humanized typing method and integrate into typeSearch</name>
  <files>utilities/src/main/java/utilities/BankSearchUtils.java</files>
  <action>
Create a private helper method `typeWithDelay(Script script, String text)` that:
1. Iterates through each character in the text
2. Uses keyboard.type() with single character strings
3. Adds random delay between characters using pollFramesHuman() with range 50-150ms

Then update typeSearch() method (around line 215) to call this new method instead of direct keyboard.type():

Replace:
```java
script.getKeyboard().type(itemName);
```

With:
```java
typeWithDelay(script, itemName);
```

The method signature:
```java
/**
 * Types text with random delays between characters for humanization.
 *
 * @param script the script instance
 * @param text the text to type
 */
private static void typeWithDelay(Script script, String text) {
    for (int i = 0; i < text.length(); i++) {
        script.getKeyboard().type(String.valueOf(text.charAt(i)));
        // human-like delay between characters (50-150ms)
        if (i < text.length() - 1) {
            script.pollFramesHuman(() -> false, script.random(50, 150));
        }
    }
}
```

Note: Only add delay between characters, not after the last character (the existing post-typing delay in typeSearch handles that).
  </action>
  <verify>Code compiles: `cd /Users/zaffre/Documents/Engineering/Projects/Scripts-Project/tidals-scripts && JAVA_HOME=$(/usr/libexec/java_home -v 17) gradle :utilities:compileJava`</verify>
  <done>typeSearch() uses per-character typing with 50-150ms random delays between characters</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild TidalsUtilities.jar</name>
  <files>utilities/jar/TidalsUtilities.jar</files>
  <action>
Build the utilities module to regenerate the jar file:
```bash
cd /Users/zaffre/Documents/Engineering/Projects/Scripts-Project/tidals-scripts && JAVA_HOME=$(/usr/libexec/java_home -v 17) gradle :utilities:build
```

Verify the jar was rebuilt by checking file timestamp.
  </action>
  <verify>ls -la utilities/jar/TidalsUtilities.jar shows recent timestamp</verify>
  <done>TidalsUtilities.jar rebuilt with humanized typing</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Code compiles without errors
- [ ] TidalsUtilities.jar rebuilt with current timestamp
- [ ] No regressions to existing search functionality
</verification>

<success_criteria>

- typeWithDelay() helper method added to BankSearchUtils
- typeSearch() calls typeWithDelay() instead of direct keyboard.type()
- Random delays of 50-150ms between each character typed
- TidalsUtilities.jar rebuilt
- All tasks committed
</success_criteria>

<output>
After completion, create `.planning/phases/11-humanized-typing/11-01-SUMMARY.md`
</output>
