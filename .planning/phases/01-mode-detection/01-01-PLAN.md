---
phase: 01-mode-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - TidalsSecondaryCollector/src/main/java/strategies/MortMyreFungusCollector.java
autonomous: true

must_haves:
  truths:
    - "Script detects Fairy Ring mode when Dramen staff equipped + bloom tool in inventory + ardy cloak equipped"
    - "Script detects Ver Sinhaza mode when Drakan's Medallion equipped + bloom tool equipped"
    - "Mode selection happens automatically without user configuration"
  artifacts:
    - path: "TidalsSecondaryCollector/src/main/java/strategies/MortMyreFungusCollector.java"
      provides: "Mode detection logic and mode enum"
      contains: "enum Mode"
  key_links:
    - from: "verifyRequirements()"
      to: "mode field"
      via: "equipment detection sets mode"
      pattern: "mode = Mode\\.(FAIRY_RING|VER_SINHAZA)"
---

<objective>
Implement auto-detection of Fairy Ring vs Ver Sinhaza mode based on player equipment.

Purpose: Enable the script to seamlessly support both teleport methods without user configuration.
Output: MortMyreFungusCollector with mode detection that sets internal mode flag based on equipment checks.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Existing implementation to modify:
@TidalsSecondaryCollector/src/main/java/strategies/MortMyreFungusCollector.java
@TidalsSecondaryCollector/src/main/java/strategies/SecondaryCollectorStrategy.java

# Reference for OSMB equipment API patterns:
@docs/api-reference.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Mode enum and detection field</name>
  <files>TidalsSecondaryCollector/src/main/java/strategies/MortMyreFungusCollector.java</files>
  <action>
Add a Mode enum to MortMyreFungusCollector with two values:
- VER_SINHAZA (existing behavior - Drakan's Medallion + equipped bloom)
- FAIRY_RING (new - Dramen staff + inventory bloom + ardy cloak)

Add these constants and field:
```java
// mode detection
public enum Mode { VER_SINHAZA, FAIRY_RING }
private Mode detectedMode = null;

// dramen staff item id
private static final int DRAMEN_STAFF = 772;
```

Place the enum inside the class (not as separate file) to keep changes minimal.
  </action>
  <verify>File compiles without errors</verify>
  <done>Mode enum and field exist in MortMyreFungusCollector</done>
</task>

<task type="auto">
  <name>Task 2: Implement mode detection in verifyRequirements()</name>
  <files>TidalsSecondaryCollector/src/main/java/strategies/MortMyreFungusCollector.java</files>
  <action>
Modify verifyRequirements() to detect mode based on equipment. Detection logic:

1. First check for Dramen staff (fairy ring mode indicator):
   - Dramen staff equipped (weapon slot)
   - Bloom tool in INVENTORY (not equipped)
   - Ardy cloak equipped (for monastery teleport return)
   → Set mode = Mode.FAIRY_RING

2. If not fairy ring mode, check for Ver Sinhaza mode (existing):
   - Drakan's Medallion equipped (neck slot)
   - Bloom tool equipped (weapon slot)
   → Set mode = Mode.VER_SINHAZA

3. If neither configuration detected → log error and return false

Key changes to verifyRequirements():
- Move equipment tab open to start
- Check for Dramen staff first (id 772)
- If Dramen staff found:
  - Check inventory for bloom tool (not equipment)
  - Check equipment for ardy cloak
  - If both present → FAIRY_RING mode
- If no Dramen staff:
  - Existing Drakan's Medallion + equipped bloom checks → VER_SINHAZA mode
- Log detected mode clearly: "detected mode: FAIRY_RING" or "detected mode: VER_SINHAZA"

Do NOT change any other methods yet - just detection. The mode flag will be used by collect/bank/return in later phases.

Important: Keep the existing bloom tool detection for VER_SINHAZA mode (checks equipment slot). For FAIRY_RING mode, bloom tool must be in inventory (since Dramen staff occupies weapon slot).
  </action>
  <verify>`osmb build TidalsSecondaryCollector` succeeds</verify>
  <done>
- verifyRequirements() detects mode based on equipment
- Mode.FAIRY_RING set when: Dramen staff equipped + bloom in inventory + ardy cloak equipped
- Mode.VER_SINHAZA set when: Drakan's Medallion equipped + bloom equipped
- Detected mode logged at startup
  </done>
</task>

<task type="auto">
  <name>Task 3: Add mode getter for future phases</name>
  <files>TidalsSecondaryCollector/src/main/java/strategies/MortMyreFungusCollector.java</files>
  <action>
Add a public getter method for the detected mode:

```java
// get detected mode (null if verifyRequirements not yet called)
public Mode getDetectedMode() {
    return detectedMode;
}
```

This allows the main script or other components to query which mode was detected.
Also add a simple check method:

```java
public boolean isFairyRingMode() {
    return detectedMode == Mode.FAIRY_RING;
}
```

Place these after the constructor.
  </action>
  <verify>`osmb build TidalsSecondaryCollector` succeeds with no errors</verify>
  <done>Mode getter and isFairyRingMode() helper available for use in later phases</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `osmb build TidalsSecondaryCollector` succeeds
- [ ] Mode enum exists with VER_SINHAZA and FAIRY_RING values
- [ ] verifyRequirements() sets detectedMode based on equipment
- [ ] Dramen staff ID (772) is defined as constant
- [ ] Detection logic is clearly commented
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Mode detection implemented and ready for use
- Existing Ver Sinhaza functionality unchanged (just refactored into mode check)
</success_criteria>

<output>
After completion, create `.planning/phases/01-mode-detection/01-01-SUMMARY.md`
</output>
