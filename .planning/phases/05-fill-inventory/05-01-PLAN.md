---
phase: 05-fill-inventory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [utilities/src/main/java/utilities/BankSearchUtils.java, utilities/jar/TidalsUtilities.jar]
autonomous: true
---

<objective>
Implement searchAndFillInventory() method that searches for an item by name and withdraws enough to fill all remaining inventory slots.

Purpose: Complete the BankSearchUtils feature set by adding fill-to-capacity functionality that uses search.
Output: searchAndFillInventory() method in BankSearchUtils, rebuilt JAR.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@utilities/src/main/java/utilities/BankSearchUtils.java
@utilities/src/main/java/utilities/BankingUtils.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement searchAndFillInventory() method</name>
  <files>utilities/src/main/java/utilities/BankSearchUtils.java</files>
  <action>
Add searchAndFillInventory(Script script, int itemId) method that:
1. Checks bank is visible (return -1 if not)
2. Gets free inventory slots via getWidgetManager().getInventory().search(Collections.emptySet()).getFreeSlots()
3. Returns 0 if no free slots
4. Gets item name from itemId using getItemManager().getItemName()
5. Uses existing typeSearch() to search for item
6. Verifies item exists in filtered results
7. If not found via search, tries scroll fallback (reuse findItemByScrolling logic)
8. Withdraws free-slots amount using bank.withdraw(itemId, slots)
9. Clears search after withdrawal
10. Returns number of slots filled, or -1 on failure

Follow BankingUtils.withdrawToFillInventory() return pattern (int representing slots filled or -1 for failure).
Add appropriate logging for each step.
  </action>
  <verify>Code compiles without errors: cd /Users/zaffre/Documents/Engineering/Projects/Scripts-Project/tidals-scripts && JAVA_HOME=$(/usr/libexec/java_home -v 17) gradle :utilities:compileJava</verify>
  <done>searchAndFillInventory() method exists with proper search, fallback, and withdrawal logic</done>
</task>

<task type="auto">
  <name>Task 2: Build and verify utilities JAR</name>
  <files>utilities/jar/TidalsUtilities.jar</files>
  <action>
Build the utilities module to produce updated TidalsUtilities.jar:
1. Run gradle build command
2. Verify JAR was updated by checking timestamp
  </action>
  <verify>JAR exists and was recently modified: ls -la utilities/jar/TidalsUtilities.jar</verify>
  <done>TidalsUtilities.jar rebuilt with searchAndFillInventory() method</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] searchAndFillInventory() method compiles
- [ ] Method follows existing patterns (Script first param, returns int)
- [ ] TidalsUtilities.jar rebuilt
</verification>

<success_criteria>

- searchAndFillInventory() method implemented
- Method uses search infrastructure (typeSearch, clearSearch)
- Method includes scroll fallback
- JAR rebuilt and available for scripts
  </success_criteria>

<output>
After completion, create `.planning/phases/05-fill-inventory/05-01-SUMMARY.md`
</output>
