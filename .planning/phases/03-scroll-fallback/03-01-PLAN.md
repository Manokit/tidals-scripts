---
phase: 03-scroll-fallback
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - utilities/src/resources/scroll_up.png
  - utilities/src/resources/scroll_down.png
  - utilities/src/main/java/utilities/BankScrollUtils.java
autonomous: false
---

<objective>
Create scroll button image resources and implement scroll detection/tap methods.

Purpose: Enable bank scrolling via image-based detection of scroll arrows when search doesn't find items.
Output: BankScrollUtils.java with scrollDown(), scrollUp() methods that find and tap scroll buttons.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scroll-fallback/03-RESEARCH.md
@docs/image.md
@docs/Finger.md
@utilities/src/main/java/utilities/BankSearchUtils.java
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <action>Capture scroll button images from OSRS bank interface</action>
  <instructions>
    Claude cannot create these images - they must be captured from the actual game client.

    1. Open OSMB client and start any script that opens the bank
    2. Open bank with enough items to enable scrolling (scroll arrows visible)
    3. Screenshot the bank interface
    4. Crop TWO images:
       - **scroll_up.png**: Just the up arrow button (top of scrollbar, ~15x15px)
       - **scroll_down.png**: Just the down arrow button (bottom of scrollbar, ~15x15px)
    5. Save to: `utilities/src/resources/scroll_up.png` and `utilities/src/resources/scroll_down.png`

    Image requirements:
    - PNG format (required)
    - Small size - just the arrow, not surrounding area (~15x15 or 20x20 pixels)
    - Clear, not blurry
    - Captured from OSRS mobile interface (not desktop)
  </instructions>
  <verification>ls utilities/src/resources/*.png shows both scroll_up.png and scroll_down.png</verification>
  <resume-signal>Type "done" when both scroll images are saved to utilities/src/resources/</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Implement BankScrollUtils with scroll detection</name>
  <files>utilities/src/main/java/utilities/BankScrollUtils.java</files>
  <action>
    Create new BankScrollUtils.java with:

    1. Static SearchableImage fields for scroll_up and scroll_down buttons (lazily loaded)
    2. init(Script script) method that loads images from src/resources/ with:
       - EuclideanToleranceComparator(15) for color tolerance
       - ColorModel.RGB for matching
    3. scrollDown(Script script) method:
       - Call init() if images not loaded
       - Use getPixelAnalyzer().findSubImages(scrollDownArrow)
       - If found, tap the button center with getFinger().tap(matches.get(0).getCenter())
       - Add human-like delay after tap (200-400ms)
       - Return true if tapped, false if button not found
    4. scrollUp(Script script) method:
       - Same pattern as scrollDown but with scrollUpArrow

    Follow existing *Utils pattern (static methods, Script as first param).
    Log each action for debugging.

    Use File path "src/resources/scroll_up.png" (relative from script execution dir).
  </action>
  <verify>Build compiles: cd tidals-scripts && JAVA_HOME=$(/usr/libexec/java_home -v 17) gradle :utilities:compileJava</verify>
  <done>BankScrollUtils.java exists with scrollDown(), scrollUp(), and init() methods</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Both scroll image files exist in utilities/src/resources/
- [ ] BankScrollUtils.java compiles without errors
- [ ] scrollDown() and scrollUp() methods use SearchableImage + PixelAnalyzer pattern
</verification>

<success_criteria>

- Human captured scroll button images
- BankScrollUtils.java created with working scroll methods
- Code follows image.md patterns for SearchableImage loading
- Compiles successfully
</success_criteria>

<output>
After completion, create `.planning/phases/03-scroll-fallback/03-01-SUMMARY.md`
</output>
